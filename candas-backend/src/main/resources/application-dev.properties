server.address=${SERVER_ADDRESS:127.0.0.1}
server.port=${SERVER_PORT:8080}

# Frontend local para desarrollo (Vite)
candas.cors.allowed-origin-patterns=${CORS_ALLOWED_ORIGINS:${CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:5173,http://127.0.0.1:5173}}
