# Copiar a .env.production para `npm run build` / preview local, o usar variables del CI.
# Debe coincidir con la URL pública HTTPS del backend (misma que en Railway).
VITE_API_BASE_URL=https://TU_BACKEND.up.railway.app
